export enum TriggerMode {
  Proactive = 'proactive',
  OnDemand = 'onDemand',
}

export enum DisplayMode {
  Tooltip = 'tooltip',
  Inline = 'inline',
  SideWindow = 'sideWindow',
  Chatbot = 'chatbot',
  Hybrid = 'hybrid'
}